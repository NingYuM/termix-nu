---
globs: "*.nu"
---

# Nushell 脚本编码约定

## 文件结构规范

### 1. 函数定义模式
- 使用 `def` 定义函数，优先使用明确的参数类型
- 函数名使用 kebab-case 命名（如 `check-assets`, `git-batch-exec`）
- 复杂函数应包含 `--help` 文档说明

### 2. 模块导入规范
- 通用工具从 [utils/common.nu](mdc:utils/common.nu) 导入
- 使用 `use` 语句导入模块和函数
- Git相关功能从 [utils/git.nu](mdc:utils/git.nu) 导入

### 3. 错误处理
- 使用 `try` 块处理可能失败的操作
- 重要操作使用 `do` 块包装
- 错误信息使用中文描述

### 4. 数据处理模式
- 优先使用管道操作 `|` 进行数据流转换
- 使用结构化数据处理，避免字符串解析
- 表格数据使用 `select`, `where`, `each` 等命令处理

## 常用工具函数

参考 [utils/common.nu](mdc:utils/common.nu) 中的通用函数：
- 环境检查和配置
- 文件操作辅助函数
- 字符串处理工具

## 项目特定约定

- 所有脚本都假设在项目根目录运行
- 使用环境变量进行配置传递
- 重要操作提供进度反馈和确认提示
